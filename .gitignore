# Logs (runtime ignored, but KEEP the committed feature-change log)
logs/*
!logs/feature-changes/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env & Secrets
.env
.env.*
!.env.example

# Claude (Personal + Session State)
.claude/settings.local.json
.claude/projects/
# NOTE: project-scope agent memory IS committed (knowledge base); only local-scope is ignored
.claude/agent-memory-local/

# Agent Artifacts
.artifacts/

# Raw Reference Capture (video/frames can be large; re-derivable, not source)
reference/

# Agent Markdown
AGENT_NOTES.md
AGENT_README.md
