# Chapter 01 — Hero + site chrome (v3 rebuild)

- **Date:** 2026-07-08
- **Author:** main (+ qa-auditor audit)
- **Type:** feat
- **Chapter/Area:** 01 Hero (+ AuroraBackground, Header, MobileMenu)

## Summary

Full-viewport hero with the reference's mixed-pairing name device ("Muhammad" General Sans medium / "Sufyan." Fraunces italic, stacked on narrow viewports), tagline + role from `profile.data`, ember scroll cue, and the canvas aurora behind it (3 additive ember blobs, upper-right weighted, drifting on `gsap.ticker`, fading to 0 as the hero scrolls past). Entrance char reveal is gated on `preloaderDone`. Site chrome shipped with it: fixed 72px Header (transparent → ink + hairline at 40px scroll, MS monogram, 4-link mono nav, hamburger) and the z-80 MobileMenu (8 staggered numbered links, Escape/scroll-lock/focus management, background inert).

## Files touched

- `src/features/home/sections/HeroSection.tsx` — new; char reveal (chars stagger .025 → .hero-item stagger .08 overlap -0.55), cue bob, ±10px pointer parallax, descender headroom `pb-[0.12em] -mb-[0.12em]` on clip wrappers, `aria-label` on the split h1 (audit F4)
- `src/features/home/components/AuroraBackground.tsx` — new; 0.5× buffer, pauses on hidden/offscreen/faded, token-driven colors (NaN guard, audit F6); reduced motion = static `accent-tint` radial gradient
- `src/components/layouts/Header.tsx` — new; ScrollTrigger class toggle; RM branch returns cleanup (audit F5)
- `src/components/layouts/MobileMenu.tsx` — new; focus → close button on open, restored to trigger on close (audit F2)
- `src/components/layouts/RootLayout.tsx` — nested inert (preloader > all; menu > header+content)
- `src/components/common/Link.tsx` — hash scroll uses `{ force: true }` so a stopped Lenis (menu open) still scrolls (audit F1, root-cause fix in the shared Link)
- `src/constants/navigation.ts` — `headerLinks` desktop subset (About/Journey/Gallery/Contact)
- `src/features/home/pages/HomePage.tsx` — composes HeroSection + temporary 150vh runway (removed at ch.02)
- `index.html` + `public/robots.txt` — meta description, theme-color, OG basics, title; robots.txt (SEO 82 → 100)

## Notable decisions

- Name deliberately stacks into two lines at display scale ("Muhammad" is 8 chars at 12vw) — matches the editorial weight of the reference even though the reference's shorter name fits one line.
- Desktop header shows a 4-link subset; all 8 chapters live in the mobile menu (and later the footer).
- Skip-to-content link (audit F7) deferred to footer polish.

## Verification

- [x] tsc + lint clean; qa-auditor static DoD all PASS; findings F1/F2/F4/F5/F6 fixed (F3 = this log, F7 deferred)
- [x] Runtime: entrance plays after preloader; aurora fades 1→0.3→0 and restores; header toggles solid/transparent both ways; menu open/close/Escape/inert/scroll-lock; menu-link scroll verified 900→0 through stopped Lenis
- [x] Reduced motion: static gradient, no split, all content visible, header always solid, native scroll
- [x] Lighthouse (desktop): A11y 100 · Best Practices 100 · SEO 100; prod trace LCP 184 ms, CLS 0.01
- [x] Console clean

## Follow-ups

- Chapter 02 Manifesto next (replaces the scroll runway).
- Skip-to-content link at footer polish (F7).
