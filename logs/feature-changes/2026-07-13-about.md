# About — persona chapter + veil landing (ch. 03, commit 3/3)

- **Date:** 2026-07-13
- **Author:** main (Claude Code, PROMPT #4 one-shot run)
- **Type:** feat
- **Chapter/Area:** 03 About

## Summary

Built chapter 03 About as the manifesto veil's landing target (PROMPT #4 §8 + PLAN v3.1 row): the whole section condenses in crisp out of the blur (autoAlpha/y/blur(14px) clear, once, `clearProps:"filter"`), then children stagger — two-tier off-axis `aboutStatement`, bio beside the rounded-top portrait, hairline mono stats row with count-up (3 · 7 · 10), MagneticButton CV link. With About in place, the manifesto's full exit grammar now verifies end-to-end: veil smears the composite → sticky releases → About scrolls in sharp; the stage hides past the manifesto and restores on the way back.

## Files touched

- `src/features/home/sections/AboutSection.tsx` — new chapter. Statement split at the comma into the two hard-aligned tiers (copy verbatim; the h2 carries the full sentence as `aria-label`, the visual tiers are `aria-hidden` `RevealText mode="lines"` spans in a nested 12-col grid). Bio cols 1–6; portrait cols 8–12 (`ParallaxImage`, `aspect-[4/5]`, scrim, `rounded-t-[2rem]`); stats `ul` with `border-y`/`border-l` hairlines, values `font-mono text-chapter`, labels `text-eyebrow uppercase`; CV via `MagneticButton` + `Link href={profile.cvUrl}` (the Link itself is the `.magnetic-label` so it counter-moves). One useGSAP (deps `[prefersReducedMotion]`, revertOnUpdate): §4.6 wrapper clear → `.about-item` stagger (top 60%) → `textContent` count-up (`snap:{textContent:1}`, 1.2s, power2.inOut, top 55%) — all `once`.
- `src/components/common/ParallaxImage.tsx` — graceful degrade for not-yet-supplied assets: `onError` hides the broken-image glyph (`opacity-0`) and the figure now carries `bg-raised`, so PLAN §8 externalities render as an editorial placeholder block. Serves 04/07 thumbnails later too.
- `src/features/home/pages/HomePage.tsx` — mount `<AboutSection />`.
- `PLAN.md` — 03 row marked shipped (as-built).

## Notable decisions

- Portrait src is the **public** path `/assets/images/portrait.webp` (consistent with `cvUrl`'s `/assets/pdf/...`), not a bundled `src/assets` import — importing a file that doesn't exist yet fails `vite build`; the public path 404s into the primitive's new graceful-hide. Swap to a bundled import when the photo arrives.
- Statement tiers avoid `display: contents` on the h2 (AT role-stripping history) — the h2 is itself a nested grid spanning cols 1–12.
- RevealText's own enter-reveal plays under the wrapper's blur-clear (both trigger ~top 78–80%, once) — reads as one "condense" gesture; no extra sequencing code.
- The CV link 404s until the PDF externality lands (PLAN §8, accepted).

## Verification

- [x] Veil → About handoff screenshot: manifesto smears out above, `03 — ABOUT` arrives crisp below
- [x] Settled About screenshot matches the reference-sparse layout (off-axis tiers, bio + portrait block, stats hairline row)
- [x] Literal page bottom: manifesto stage hidden (`opacity 0`), nothing fixed lingers over About; back to top: stage + exact slot clip restored (the `end:"max"`-class regression)
- [x] Menu → About anchor nav: Lenis smooth-scrolls through the pinned manifesto, lands `#about` at viewport top 0, URL `/#about`
- [x] Reduced motion (puppeteer): About fully visible, static 3/7/10, statement aria-label readable, zero canvases, zero page errors
- [x] `npm run lint` + `npm run build` green
- [x] Lighthouse (production preview, full page): A11y 100 · Best Practices 100 · SEO 100

## Follow-ups

- Externalities owed (PLAN §8): portrait photo (`public/assets/images/portrait.webp`) and CV PDF (`public/assets/pdf/Muhammad Sufyan CV.pdf`).
- Next per PLAN v3.1: 04 Project/Craft.
