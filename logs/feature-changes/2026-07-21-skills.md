# 06 Skills — all-open grouped accordion + scrubbed ember arrow

- **Date:** 2026-07-21
- **Author:** main
- **Type:** feat
- **Chapter/Area:** 06 Skills

## Summary

Built chapter 06 per the owner brief (supersedes PLAN v3.1 decision 10's scrubbed
sequential accordion): left sticky column (eyebrow `06 — TOOLKIT`, uppercase
`profile.aboutStatement` via `RevealText`, hairline, CONTACT ME ✦ → `#contact`,
ember arrow lane) + right **all-open** 6-group accordion (shadcn/Radix
`type="multiple"`) with monochrome Simple Icons logos. The arrow is a damped
x-scrub of section progress (gallery damp-applier pattern); freeze-on-pause and
exact reverse retrace verified numerically.

## Phase 0 dissection findings (reference/skills-refine.mp4, 8 fps, 239 frames)

- Beat table re-derived programmatically (orange-blob centroid + right-zone
  density): arrow enters from below t≈3.3–4.8 s settling at lane y≈65svh,
  x≈14vw; scrubs to ≈28vw sustained (freeze verified t≈9–12.5; reverse retrace
  t≈18–20; forward/back play t≈20–26); exits upward with x still drifting to
  ≈38vw; next (light) chapter floods in at t≈17.
- **Key insight: x stays fixed during entry and keeps drifting during exit** →
  the reference's enter/exit is the sticky lane scrolling in/out of the
  viewport. Implemented accordingly: layout owns y (sticky h-svh column + flow
  placement), the engine owns x only — no phase spans, no state machine.
- Anatomy (f050/f120): left column sticky; statement = heavy uppercase grotesk;
  hairline between statement and CONTACT ME ✦; arrow = solid thick-shaft
  triangular-head →, ~9vw wide. Accordion rows = plain muted text lists, no
  item dividers, groups separated by whitespace, thin +/− far right. The
  reference shows NO logos and NO level chips (owner brief adds logos; level
  chips omitted per brief D4 anatomy — data keeps levels).
- Reference taxonomy includes "System & Security" (Linux, Bash, Nmap…) — NOT
  transcribed; owner taxonomy applied instead (grep-verified absent).

## Files touched

- `src/features/home/sections/SkillsSection.tsx` — NEW: section, arrow engine, entry stagger
- `src/features/home/utils/skills.tunables.ts` — NEW: `SKILLS_SECTION` (travelVw 24, damp 5, entry stagger, logo size)
- `src/features/home/data/skills.data.ts` — ADD `skillGroups` presentation regroup (derived from verbatim `skills`/`tools`; PRD arrays untouched)
- `src/features/home/utils/tech-icons.ts` — EXTEND `TECH_ICONS` +13 entries (now serves 04 badges + 06 logos)
- `src/components/ui/accordion.tsx` — NEW via shadcn, restyled to tokens; +/− indicator per reference anatomy
- `src/styles/globals.css` — `--animate-accordion-down/up` + keyframes (`--dur-fast`/`--ease-out` tokens)
- `src/features/home/pages/HomePage.tsx` — insert `<SkillsSection />`; 05 Journey slot comment kept

## Notable decisions

- **Taxonomy mapping** (owner target → PRD): Frontend 12 · Backend 4 ·
  Databases 2 · DevOps & Tools 4 (VS Code, GitHub, XAMPP, Git) · Mobile 4
  (React Native, Flutter, Dart + Android Studio) · Design 1 (Figma).
  **"Animation & 3D" DEFERRED** — PRD has no animation/3D skills; options at
  gate: drop / merge / owner supplies data. Items regrouped, never invented.
- **Logo provenance** (owner decision this session): Simple Icons (CC0) via the
  installed `react-icons` (MIT) package — same pipeline as 04's tech badges;
  no raw-SVG second pipeline. Monochrome via `currentColor` (rows are
  `text-muted`). The brief's `hoverBrand` micro-delight flag was DROPPED (qa
  F1): react-icons carries no brand hex and raw hex is banned — a brand-color
  hover needs a deliberate color-map decision first. **Visual Studio Code has
  no Simple Icon** (mark removed upstream) → renders name-only with an empty
  icon slot (never a wrong-brand substitute).
- Arrow enter/exit ride the sticky column (dissection evidence above) — the
  brief's three-phase engine collapsed to one damped x-scrub
  (`start: "top top"`, `end: "bottom top"`); freeze/retrace inherent.
- Arrow lane is flow-positioned after the CTA: ≈66svh on tall viewports,
  clamps below the CTA on short ones (700px verified, no overlap, no clip).
- Arrow omitted below `lg` (D5 judgment: the lane needs the sticky viewport
  column; single-column flow has nowhere honest to put it).
- Radix keeps closed panels mounted-hidden and plays the `accordion-up`
  keyframe before hiding (verified mid-animation); `aria-controls` drops while
  closed — standard Radix/shadcn behavior.

## Verification

- [x] `npx tsc --noEmit` clean
- [x] `npm run lint` clean
- [x] `npm run build` clean (chunk-size warning = known deferred bundle split)
- [x] reduced-motion checked (playwright-core: arrow static, all open, no Lenis, 0 page errors)
- [x] scrub proof: frozen x across 2 s (107.51 = 107.51); retrace Δ0.05px; up-pass Δ0.04px
- [x] ARIA: 6 × `<h3><button aria-expanded aria-controls>`, panels labelled regions, native-button keyboard
- [x] captures in `.artifacts/qa-skills/` (S1/S2×3/S3 down+up, fresh-load, mobile 390, short 700, reduced-motion)
- [x] Lighthouse (prod preview): perf 62 (carried page ledger, = ch.07; final-QA
      item) · a11y 98 (sole failure = pre-existing ch.04 heading carry) ·
      best-practices 100 · SEO 100
- [x] qa-audit 2026-07-21: 0 blockers/majors; both lows fixed same-session
      (F1 dead tunables deleted, F2 `onRefresh` resize re-projection added)

## Owner revisions (same session, pre-commit)

- Section order: Projects → **Gallery → Skills** (owner request; HomePage +
  `navLinks` reordered — scroll HUD/SiteMenu follow the array). Eyebrows
  renumbered by owner: Gallery `06 — Selected Work`, Skills `07 — Toolkit`.
- Arrow glyph: solid triangular-head → **stroke arrow** (line shaft + open
  chevron, rounded caps) per owner reference image; still ember/currentColor,
  same engine.
- Type scale (two owner passes): items `text-item` → `text-body`, logos
  20 → 16px (kept); statement + group titles briefly dropped to `text-item`,
  then restored to `text-statement` on owner request — with the taller
  8-line statement the left column re-balanced (`pt-[8svh]`, CTA block
  `mt-8 pt-6`, arrow `mt-6`, lane ≈78svh) and a height-scoped downshift added
  (`lg:[@media(max-height:768px)]:text-item`) so the ≤768px-tall lane clamps
  instead of pushing the arrow below the fold (700h re-verified). Statement
  copy moved off the About-duplicate to a new `Profile.skillsStatement` —
  persona re-voiced into the reference's three-clause shape (role + location
  + tagline facts only; the reference image's own copy is NOT transcribed —
  wrong facts). Final pass: group titles nudged −6px via tunable
  `accordion.titleSize` (`calc(var(--text-statement) - 6px)` — token-derived,
  unitless lh/tracking still apply).
- **Logos switched to owner-supplied SVGs** (owner request): full-color brand
  files in `src/assets/icons/` (SVG Repo), mapped by
  `utils/skill-icons.ts` (`import.meta.glob` URL map + name aliases,
  decorative gapura/ornament/arrow files excluded from the glob — they'd add
  ~1.2 MB to dist), rendered through the `Image` primitive
  (`bg-transparent` overrides the skeleton backdrop). SUPERSEDES both the
  monochrome treatment and the react-icons pipeline for 06 (the
  `TECH_ICONS` +13 extension was reverted — that map is 04-only again).
  **Visual Studio Code now has an icon** — the empty-slot fallback remains
  as a guard but no item hits it. All 27 icons verified loading (0 broken;
  small files inline as data URIs, 5 larger ones emit hashed assets).

## Follow-ups

- "Animation & 3D" deferred group — owner call at the gate.
- Level chips omitted (brief D4 anatomy + reference frames) — data keeps
  levels if the owner wants them surfaced later.
- 05 Journey still slots between 04 and 06 (`HomePage.tsx` comment).
