---
name: site-chrome
description: Header/SiteMenu/RootLayout structure, z-index scale, preloader inert pattern, and per-chapter section conventions (hero, craft, full-bleed pattern, RevealText nesting limit)
metadata:
  type: project
---

# Site chrome & chapter conventions (built 2026-07-07)

> **⚠️ HARD RESET (2026-07-07):** the chrome files were deleted with the rest of `src/`; this doc is the spec-of-record for rebuilding them.

**Why:** chapters must slot into this frame without re-deciding z-order, header offset, or a11y patterns.
**How to apply:** new sections go in `features/home/sections/`, get an `id` matching [[content-data-layer]] navLinks anchors, and render inside HomePage's `<Box as="main" id="main">`.

- **z-scale (arbitrary z-[] is the house style):** Header `z-[60]` < SiteMenu `z-[80]` < Preloader `z-[90]` < Cursor `z-[100]`.
- **RootLayout** wraps `<Header /> <Outlet /> <SiteMenu />` in a Box with `inert={!preloaderDone || undefined}` (React 19 boolean inert; Box spreads native props) — F1 fix so nothing behind the preloader is focusable. Preloader sets `preloaderDone` immediately on reduced-motion/session-skip, so content is never wrongly inert. Devtools stay outside the inert wrapper.
- **Header (2026-07-08):** fixed, `h-18` (=72px), transparent → solid on scroll (ScrollTrigger classList toggle at scrollY > 40, inside Header itself). **No inline nav links** — all nav lives in SiteMenu, opened by a glass "Menu" pill (all viewports): shadcn Button ghost restyled `rounded-full border border-paper/15 bg-paper/10 backdrop-blur-md font-mono text-eyebrow uppercase`, wrapped in `MagneticButton` with a `.magnetic-label` span. Glass pattern = opacity modifiers on token colors (no raw hex); `rounded-full` is a user-approved exception to the small-radius rule. shadcn ghost hover must be overridden (`hover:bg-paper/20 hover:text-paper dark:hover:bg-paper/20`) — default `hover:bg-accent` paints the accent. `headerLinks` was removed from `constants/navigation.ts` (only `navLinks` remains).
- **SiteMenu** (renamed from MobileMenu 2026-07-08; serves all viewports): `role="dialog" aria-modal aria-label="Menu"`; Escape closes; focus → close button on open, restores to opener on close; scroll-lock via `lenis.stop()` + html overflow hidden. **Animated open/close:** curtain `yPercent -100→0` (power4.inOut, 0.8s) then `.menu-link` stagger; close reverses (links `yPercent→100`, curtain up) and unmounts on complete. Stays mounted through exit via a `rendered` state **adjusted during render, not in an effect** — `react-hooks/set-state-in-effect` lint rejects the effect version; reduced motion flips `rendered` synchronously (instant mount/unmount, no transforms).
- **HeroSection (`#intro`):** `min-h-screen flex-col px-page-x pt-18` (pt offsets the fixed header); content block `flex-1 justify-center`; scroll cue (`Link href="#manifesto"`, mono text-meta text-accent, ↓) in normal flow at the bottom with `pb-10` — no absolute positioning. Name = `Heading variant="display"` with each word in a `Box as="span" className="block"` (split-ready for motion). Stats row = `ul` flex-wrap, `border-l border-line pl-8` separators from item 2 on. Hero tagline microcopy (approved re-voice of PRD bio): "Building digital applications that help people — across web and mobile." No portrait (no asset in PRD).
- **Standard chapter shell (Manifesto/Craft precedent):** `<Box as="section" id=… className="px-page-x py-section">` → `ChapterEyebrow` → title → content. Chapter titles: `RevealText as="h2" mode="lines" className="font-display text-chapter text-paper"` (plain text child only).
- **RevealText limit:** split-type does NOT support nested elements — `RevealText` only wraps plain text. Structural "line" reveals (flex rows, lists) replay its recipe in a scoped `useGSAP`: `overflow-hidden` on the static parent (borders stay there), inner row `gsap.from(yPercent: 100, stagger: 0.08, scrollTrigger { start: "top 80%", once: true })`, skipped under `usePrefersReducedMotion`. See `PillarBlock`/`ManifestoSection`.
- **Full-bleed inside a padded section:** `-mx-page-x` — Tailwind v4 generates negative utilities from `--spacing-page-x` (verified in dist CSS). Same token works as pillar-grid gutter (`gap-x-page-x`). Craft marquee band: `-mx-page-x border-y border-line` + one span of `names.join(" · ")` with trailing separator for a clean loop seam.
- **PillarBlock** (`features/home/components/PillarBlock.tsx`): `{ index: string; title: string; skills: string[]; description?: string }` — editorial stack list with hairline rows; levels cross-referenced from PRD `skills` via exact name + alias map (`React→React JS`, `Tailwind→Tailwind CSS`); unmatched names (shadcn/ui, Livewire) render no level.
- **Journey timeline rail pattern** (`JourneySection` + `JourneyEntry { item: JourneyItem }`): wrapper `relative`, rail `absolute inset-y-0 left-0 w-px bg-line` drawn via `scaleY 0→1, transformOrigin top, ease "none"` scrub (`start "top 80%" end "bottom 60%"`, no pin); entries `li relative pl-8` with 10px node `size-2.5 -translate-x-1/2 top-1.5` centered on the rail, revealed per-entry `autoAlpha 0→1 y 24→0` (`"top 85%"`, once, forEach). **Award dots = `bg-accent`, work/education = `bg-faint`** (decided). Section owns all motion via hook classes (`.journey-rail`/`.journey-entry`); JourneyEntry is purely presentational.
- **Heading default-variant trap:** `Heading level={n}` default sizes include responsive `md:text-*` that survive cn-merge over token sizes (`text-item` only replaces the base class). For token-sized h3s use `RevealText as="h3"` / `Box as="h3"` + token classes (PillarBlock/JourneyEntry pattern); `variant="display"` is the only token-mapped variant so far.
- `revertOnUpdate: true` is house style on every `useGSAP` in sections/components.
