# Preloader — name-as-shared-element morph (2026-07-16, supersedes the three-act Welcome intro)

`name` prop REQUIRED (`Profile["heroName"]`, passed from `__root.tsx` — common/ must not import features); runs on **every** load. ALL numbers in `src/utils/preloader.tunables.ts` (`PRELOADER_REFINE`).

- **P0/P1**: centered `.pre-wordmark` (two `.pre-word` spans reusing `text-hero-line` + `font-display-lead/tail` classes, font-size overridden INLINE so em-based tracking/leading keep the morph glyph-exact) enters as a SplitType char cascade — chars rise UNCLIPPED from below (`y: chars.rise`) with a settling rotation, `stagger: { from: "center" }` over the combined chars (lead word fills right→left, tail left→right, per the reference) — and breathes (`scaleFrom→scaleTo` yoyo) through a REAL loading gate = `document.fonts.ready` + `useUIStore.auroraReady` + hero-words-in-DOM poll + `hold.min` floor, HARD-capped at `hold.max` (cap force-opens mid-breath); normal path opens only at a `scaleTo` breath boundary (onRepeat parity).
- **P2**: per-word FLIP — gBCR both sides, tween `x/y` delta + `scale = target.width/current.width`, `transformOrigin:"0 0"` (works for ≥lg one-line AND <lg stacked rows; measure INSIDE the door-open call so it's resize-fresh; mid-morph resize snap-completes the swap).
- **P3**: two sheets — `.pre-sheet-ember` (`bg-accent`) wipes bottom→top (`yPercent 100→0`, `sheets.emberIn`, `power4.inOut`) ending exactly at the landing → `sheets.hold` full-cover → `.pre-sheet-ink` (`bg-ink`) wipes bottom→top; the ATOMIC cut fires at ink full-cover — one timeline `call()` sets h1 words `autoAlpha:1`, wordmark + BOTH sheets hidden, overlay bg transparent, `setPreloaderDone(true)` (fires at the REVEAL, not overlay end; the cut is invisible because the ink sheet matches the page bg). The wordmark rides ABOVE both sheets the whole way (white on red then ink — NO cyan beat, owner decision).
- Overlay outlives the reveal by `(chrome.order.length−1)×stagger` so unmount (→ scroll-lock cleanup `lenis.start()`) lands at last-chrome-group-start. Overlay is `pointer-events-none` (inert + Lenis lock do the blocking).
- Reduced motion: plain ink cover, fonts-gated ≤ `reducedMotion.holdMax`, single cut, never locks scroll.
- Gate timers/unsubs are cleanup-registered (StrictMode remounts must not fire stale swaps).
