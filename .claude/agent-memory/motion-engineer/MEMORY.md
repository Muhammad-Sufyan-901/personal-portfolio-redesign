# Motion Engineer — Project Memory

> **STATUS (2026-07-08, post B0 re-bootstrap):** the motion foundation was **rebuilt** per PLAN v3: `lib/gsap.ts`, `SmoothScrollProvider`, hooks, store, and all 8 motion primitives (RevealText, ParallaxImage, Marquee, MagneticButton, ChapterEyebrow, Cursor, Preloader, PathDraw) exist again at the documented paths with the documented APIs. **Section choreography rebuilt through chapter 02** (00 Preloader, 01 Hero, 02 Manifesto shipped; About→Footer still to build on the 10-chapter map). Chapter 01 SHIPPED + REVISED (2026-07-08): HeroSection reference-exact layout — tagline top-left (italic `taglineEmphasis` substring from profile.data), name in **two giant stacked rows** (2026-07-08: `--text-hero` = `clamp(3.5rem,16vw,19rem)`, first name `self-start` / surname `self-end`, always flex-col — a shared row caps at ~10vw for the 15-glyph name, and the `overflow-hidden` clip wrappers zero flex `min-width:auto` so oversizing silently SHRINKS+CLIPS words: re-check `scrollWidth <= width` after any size bump; whitespace-nowrap on word spans; clip wrappers need `pb-[0.12em] -mb-[0.12em]` descender headroom at lh 0.95; `aria-label` on the split h1), hairline bottom bar (role · socialLinks · anchors). AuroraBackground = **React Bits ogl shader** (dep `ogl`): uTime from gsap.ticker (never its own rAF), colorStops from computed accent tokens at mount, perf pauses + ScrollTrigger fade + WEBGL_lose_context cleanup; reduced-motion/WebGL-fail → static token gradient. Header scroll-state (RM branch returns a classList cleanup — classList isn't reverted by revertOnUpdate), MobileMenu (focus → close btn on open, restore to trigger on close). **`Link` hash scroll passes `{ force: true }`** — a stopped Lenis (menu open) silently swallows scrollTo otherwise. Chapter 00 SHIPPED (2026-07-08): Preloader/Cursor mounted in `__root.tsx`; RootLayout sets `inert={!preloaderDone}` on page content while the curtain is up. Cursor: hidden until first pointermove (autoAlpha 0 at init), fades out on `documentElement` pointerleave. **Every useGSAP with `prefersReducedMotion`-derived deps needs `revertOnUpdate: true`** (3rd-recurrence audit finding — cleanup is otherwise deferred to unmount and never-unmounting components leak listeners). Display face is **Fraunces again** (grotesk swap reverted, PLAN v3 #5); accent is ember, applied.

## Motion architecture (single sources) — prior build 2026-07-07 (deleted in reset; rebuild to this spec)
- GSAP registered ONLY in `src/lib/gsap.ts` (registers ScrollTrigger, defaults `ease: "power4.out", duration: 0.8`; exports `gsap`, `ScrollTrigger`). No other file imports `gsap` or `gsap/ScrollTrigger`.
- Lenis instantiated ONLY in `src/providers/SmoothScrollProvider.tsx` (lerp 0.09, wheelMultiplier 1, smoothWheel, syncTouch false): `instance.on("scroll", () => ScrollTrigger.update())` (arrow wrapper — direct handoff fails strict TS), `gsap.ticker.add(t => lenis.raf(t*1000))`, `lagSmoothing(0)`; exports `LenisContext`; destroys + removes ticker on cleanup. Under reduced motion Lenis is NEVER created → `useLenis()` (in `src/hooks/useLenis.ts`) returns null.
- No manual resize-refresh listener — ScrollTrigger auto-refreshes on resize. Route-change refresh not wired (single-page site); add only if multi-page ships.
- Provider composition: `src/providers/AppProviders.tsx` = ThemeProvider(defaultTheme="dark") → TooltipProvider → SmoothScrollProvider. `main.tsx` = StrictMode → AppProviders → RouterProvider.
- Every animation in `useGSAP(() => {…}, { scope: ref, dependencies: [...] })`. Returning a function from the useGSAP callback registers a context cleanup (used for `split.revert()`). `invalidateOnRefresh: true` on scrubs.

## Reduced motion (mandatory pattern)
- `src/hooks/usePrefersReducedMotion.ts` — `useSyncExternalStore` on `(prefers-reduced-motion: reduce)`, live-updates on change.
- Pattern: early-return inside the useGSAP callback (content renders in final static state) + conditional classes via `cn()` (e.g. ParallaxImage drops `scale-[1.2]` headroom). Cursor/Marquee return alternate JSX (null / single static track).
- Touch detection: `matchMedia("(hover: hover)")` inside useGSAP (MagneticButton) or once via lazy `useState` (Cursor: `(hover: hover) and (pointer: fine)`).
- `src/hooks/useIsomorphicLayoutEffect.ts` exists for effect-time DOM work.

## Primitive APIs (src/components/common/, all exported from barrel index.tsx)
- `RevealText` — `{ children, mode?: RevealMode = "lines", as?: "div"|"p"|"span"|"h1"–"h6" = "div", className?, delay?, stagger? }`. split-type per mode: lines→"lines", words→"lines,words", chars→"lines,words,chars". Lines mode wraps each line in a created `overflow-hidden` div (`split.revert()` restores innerHTML, removing wrappers); words/chars add `overflow-hidden` to line els. `gsap.from(targets, { yPercent:100, stagger 0.08/0.04/0.025, scrollTrigger: { start: "top 80%", once: true } })`. No re-split on resize (once-reveals).
- `ParallaxImage` — `{ src, alt, width?, height?, aspect? (className), parallax?: ParallaxConfig, withScrim?, className? }`. Figure clip `inset(100% 0 0 0)→inset(0)` (1.2s, once) + img `yPercent from→to` scrub. Raw `<img>` with `scale-[1.2]` headroom (common/ exempt; existing `Image` skeleton wrapper fights the clip). Scrim = `bg-linear-to-b from-transparent from-40% to-ink/65`.
- `Marquee` — `{ children, speed? = 30 (s/loop), reverse?, pauseOnHover? = true, className? }`. Two `.marquee-track` flex tracks (2nd `aria-hidden`), `xPercent 0→-100` repeat -1; tween in a ref, paused/played by React pointer handlers.
- `MagneticButton` — `{ children, className?, strength? = 12 }`. `gsap.quickTo` x/y on wrapper; `.magnetic-label` span counter-moves ×-0.35; spring back `elastic.out(1, 0.4)`.
- `PathDraw` — `{ d, trigger?: RefObject<HTMLElement|null> (defaults own wrapper), strokeWidth? = 3.5, className?, viewBox? = "0 0 100 100", preserveAspectRatio? = "none", scrub? = true, start? = "top 80%", end? = "bottom 60%" }`. Bold path draw (§7.2): `getTotalLength()` → set dasharray=dashoffset=length → tween dashoffset→0, ease none, scrubbed. `stroke="currentColor"` + wrapper `cn("text-accent", className)` (token-driven, re-theme-safe); `strokeLinecap="round"`, `vectorEffect="non-scaling-stroke"` so `preserveAspectRatio="none"` stretch keeps stroke width. svg `aria-hidden focusable="false"`. Reduced motion: early return — no dash ever set → renders fully drawn. Built for Journey rail upgrade + Selected Work; not wired into sections yet.
- `ChapterEyebrow` — `{ index, label, className? }`, NOT animated. `font-mono text-eyebrow`, `text-accent` index, muted em-dash + uppercase label.
- `Cursor` — no props. 8px dot (`h-2 w-2 bg-paper`) + 40px ring (`h-10 w-10 border-paper/50 mix-blend-difference`); centered via `gsap.set({ xPercent:-50, yPercent:-50 })` (don't use Tailwind translate — gsap x/y would stack oddly). quickTo follow (dot 0.12s, ring 0.45s); `pointerover` → `closest("a, button, [data-cursor]")` → ring scale 1.6 (2.4 + label when `data-cursor` set). Toggles `cursor-none` on `<html>` while mounted+enabled. Returns null on coarse pointer / reduced motion. Mounted in `__root.tsx`.
- `Preloader` — no props (**no longer shows `siteConfig.name`** — reworked 2026-07-08 into a three-act "Welcome" intro; `siteConfig` import removed). Runs on **every** load/refresh (no sessionStorage gate). Timeline: **Act 1** "Welcome" mask-reveal (yPercent 100→0) centered inside an SVG circular ring (`.pre-ring-progress`, `RING_RADIUS=54`, stroke `--color-accent`) driven by one proxy tween `{value}` 0→100 (2s) whose `onUpdate` writes both `.pre-counter` textContent (`${pct}%`, below "Welcome") and the ring's `strokeDashoffset = RING_CIRCUMFERENCE * (1 - value/100)` (clockwise fill from 12 o'clock via `-rotate-90` on the `<svg>`) — **Act 2** `.pre-welcome` (bg-ink) fades (`autoAlpha`, 0.4s) while `.pre-ember-l`/`-r` (two `bg-accent` halves) + `.pre-ember-content` slide up together (`yPercent 100→0`, 1s `power4.inOut`; parked at `yPercent:100` via `gsap.set` before the timeline builds, not `.from()`, since they animate mid-timeline) revealing "To My Portfolio" (`SplitType` chars, `text-ink` on ember, `gsap.set(chars,{yPercent:100})` pre-hide → `.to(yPercent:0, stagger:0.03)`) — **Act 3** ember content fades then `.pre-ember-l`/`-r` split-curtain `xPercent` ∓100 (1.1s `power4.inOut`) revealing the site underneath (same split idiom as the old single-act version, now applied to ember halves instead of ink halves). onComplete → `setPreloaderDone(true)` + unmount (null); `split.revert()` in cleanup. Reduced motion: renders null, signals done in a layout effect. Scroll-locks via `lenis?.stop()/start()`. Mounted in `__root.tsx`. Full choreography + rationale: `logs/feature-changes/2026-07-08-preloader-welcome-ember.md`.

## Chapter choreography patterns (built)
- **Hero entrance gating** (`src/features/home/sections/HeroSection.tsx`): one useGSAP, `dependencies: [preloaderDone, prefersReducedMotion]`, `revertOnUpdate: true`. Flow: split name chars → `gsap.set` initial hide (NEVER CSS-hide — JS-dead/reduced-motion must stay visible) → if `!preloaderDone` stop (curtain reveals a blank stage) → else timeline: chars `yPercent→0` (1s, stagger 0.025) + `.hero-item` (tagline/role/stats/cue) `autoAlpha/y` stagger 0.08 overlapped `-=0.55`; cue bob `y:6` sine yoyo repeat -1; ±10px quickTo name parallax on `(hover: hover)`. `revertOnUpdate` makes the preloaderDone flip re-split clean markup instead of splitting split DOM.
- **Hero aurora** (`src/features/home/components/AuroraBackground.tsx`, section-local, no props): canvas under `blur-3xl scale-110` inside `Box absolute inset-0 -z-10 pointer-events-none overflow-hidden` (hero section got `relative isolate`, aurora is its first child). 3 additive (`"lighter"`) radial blobs, upper-right weighted, drifting on sin/cos of `gsap.ticker` time (seconds); colors read at mount from computed `--accent`/`--accent-deep` via local hex→rgba helper (theme-flip re-read NOT wired). Perf: buffer at 0.5× CSS px (single `RESOLUTION` knob, also covers the DPR≤1.5 cap), tick early-returns on `document.hidden` (checked per tick — no listener) / IntersectionObserver `offscreen` / `faded` from the scroll-fade ScrollTrigger (`trigger: el.closest("section")`, `top top → bottom top`, scrub, wrapper `autoAlpha→0`, `faded = progress === 1`); ResizeObserver sizes the buffer (fires on observe = initial sizing). Reduced motion: alternate JSX — static `bg-[radial-gradient(ellipse_at_75%_20%,var(--accent-tint),transparent_60%)]`, no canvas, zero JS.
- **Name split trick**: hero word spans are `block overflow-hidden` in JSX (they ARE the clip wrappers) → split `types: "chars"` only; split-type ≥0.3 handles nested spans.
- **Header removed 2026-07-08** (navbar deleted — see `logs/feature-changes/2026-07-08-navbar-removal-hero-menu-popout.md`): the old `Header.tsx` scroll-state pattern (`ScrollTrigger.create({ start: 40, end: "max", onToggle })` toggling `["bg-ink","border-line"]`) no longer exists; replaced by `MenuButton`/`MenuPopout` below.
- **Menu chrome, post-navbar-removal** (`src/components/layouts/MenuButton.tsx` + `MenuPopout.tsx`): `MenuButton` is the extracted prop-less glass pill (`MagneticButton` + shadcn `Button`, `setMenuOpen(true)`) — placed by its caller, not self-positioned. Used inline in `HeroSection`'s top row (scrolls away with the hero) AND inside `MenuPopout`, a `fixed top-6 right-page-x z-[60]` wrapper that pops it into the top-right corner past 0.45·innerHeight (`ScrollTrigger.create({ start: () => window.innerHeight * POPOUT_START, end: POPOUT_END, onToggle })` — retuned from 1.0·vh when the manifesto seam started fading the hero MenuButton at ~0.42·vh, `back.out(2)` in / `power2.in` out on `autoAlpha`+`scale`+`y`). **Gotcha: never use `end: "max"` on a no-`trigger` ScrollTrigger meant to stay active through the rest of the page** — verified via chrome-devtools MCP (wheel-scroll to the literal document bottom) that GSAP flips `isActive` back to `false` exactly when `progress` hits `1` at that boundary, so a fixed/persistent trigger vanishes right at the footer. Fix: a large fixed `end` (e.g. `100_000`) no real scroll reaches. `Header`'s old `end: "max"` never hit this because its `start: 40` meant the toggle only needed to be *entering*, not staying active through the page end.
- **SiteMenu reveal** (`src/components/layouts/SiteMenu.tsx`, renamed from MobileMenu 2026-07-08, now all viewports — header nav links removed in favor of a glass "Menu" pill in `MagneticButton`): stays mounted through exit via `rendered` state adjusted **during render** (`react-hooks/set-state-in-effect` bans the effect version). Open: curtain `fromTo(el, yPercent -100→0, power4.inOut 0.8s)` then `.from(".menu-link", { yPercent: 100, stagger: 0.06 }, "-=0.25")`; close: links `to yPercent 100` then curtain back up, `onComplete: setRendered(false)` unmounts; `li` wrappers `overflow-hidden`. Reduced motion: instant mount/unmount, no tweens.
- MenuPopout z-[60] < SiteMenu z-[80] (fits z-scale below).
- **Manifesto = the WebGL MacBook scroll-story** (2026-07-13, PROMPT #4 — supersedes keyboard disintegration `376f4c0` and scroll-fill `c14474e`). Hero-slot window → T1 seam (pin `#hero`, clip-path expansion) → T2 channels master over 520vh (`-mt-[100svh]` bridges the pin-spacer dead zone) → lid/yaw/statement/veil. **Full architecture, trigger configs, measurement rules, stacking/backdrop traps, and the R3F↔GSAP island pattern (frameloop="never" + ticker→advance, damp-in-useFrame, hinge-rig gotchas, lazy-boundary rules): see [manifesto-3d.md](manifesto-3d.md).** Headline traps for quick recall: shape-match clip strings; NO invalidateOnRefresh on plain-object channel tweens; accumulate offsets (never gBCR) for the slot; `autoAlpha` not `display:none` on the GL stage; never fade the veil's ancestor (backdrop root); ms-vs-seconds (advance wants ms, damp wants seconds); judge rig poses by renders, never rotated-subtree Box3.
- **Pin + Lenis**: Lenis drives native window scroll (no transform mode), so ScrollTrigger pins/fixed positioning just work; no `anticipatePin`, no manual resize listener needed.
- TODO when chapters 03–06 exist: scroll-spy accent active-nav dot (needs section anchors; deliberately not built with Hero).

## Motion tokens (design_system §7; live in src/styles/globals.css @theme)
- `--ease-out` ≈ `power4.out` (the gsap default), `--ease-inout cubic-bezier(0.83,0,0.17,1)`; durations 0.4/0.8/1.2s; Lenis lerp 0.09.
- Type-scale utilities carry size+leading+tracking: `text-display/chapter/statement/item/body/eyebrow/index/meta`. Accent is EMBER `#E8380F` (live in globals.css since the re-theme; older "still cobalt" notes are stale). `--spacing-manifesto-slot` sizes the hero window slot (`h-manifesto-slot`); `:root --veil-blur` is the manifesto veil's tweened var.
- Avoid the `Text` component for token type styles: its variant baseline (`text-base leading-relaxed text-foreground`) collides with custom `text-*` tokens in twMerge (unknown token classes get misclassified). Use `Box` + explicit classes.

## State & types
- `src/store/useUIStore.ts` — zustand `{ preloaderDone, menuOpen, setPreloaderDone, setMenuOpen }` only. `preloaderDone` is the Hero timeline's start cue.
- `src/types/motion.ts` — `RevealMode = "lines"|"chars"|"words"`, `ParallaxConfig { from? = -8, to? = 8 }`.
- `src/routes/__root.tsx` — wiring-only `RootComponent` = `<Preloader/> + <Cursor/> + <RootLayout/>`; local unexported component keeps react-refresh lint happy.
- z-scale convention: page content ≤ z-50 < preloader `z-[90]` < cursor `z-[100]` (cursor always highest).
- Still to build: `PageTransition` (only if multi-page ships).

## Tooling (installed 2026-07-06)
- **context7 MCP** (root `.mcp.json`) — current GSAP/ScrollTrigger/Lenis docs; prefer over memory for API details. No GSAP-specific MCP exists (only obscure community ones — skipped).
- **design-taste-frontend** skill (`.claude/skills/design-taste-frontend/`) — MOTION_INTENSITY/VARIANCE/DENSITY dials + GSAP patterns; **impeccable** has `/impeccable animate` for motion passes.

## Decisions log (durable facts only)
- 2026-07-07: theme default flipped `system`→`dark` in AppProviders (dark-first identity; storageKey unchanged `vite-ui-theme`).
- 2026-07-07: `LenisContext` export in SmoothScrollProvider carries an eslint-disable for `react-refresh/only-export-components` (context must live with its owner).
- Known gap (deliberate): native `cursor: pointer` still shows over links while custom cursor is active; add a scoped `* { cursor: none }` in QA if it bothers.
- 2026-07-07 (kit enrichment): design_system v2 is authoritative — accent = **ember `#E8380F`** (cobalt in globals.css is pre-migration; token names unchanged so primitives are unaffected). v2 motion work still to implement: **bold path draw** (thick 3–4px organic SVG rail via stroke-dashoffset scrub, replaces Journey's 1px scaleY rail; Aceternity Timeline is the sketch), hero **aurora** glow (React Bits GSAP variant as starting point), optional footer **ornament converge**. Borrowed components follow `.agents/skills/animated-ui-references` — never install framer-motion.
