# Motion Engineer — Project Memory

> **STATUS (2026-07-26):** motion foundation rebuilt per PLAN v3; chapters **00–07 ALL shipped** (Journey landed 2026-07-22; page order Projects(04) → Gallery(06) → Skills(07) → Journey(08, eyebrow renumbered from 05); **09 Articles landed 2026-07-26** — pinned horizontal scrub rail, see below; **Contact now takes 10** (was 09 — Articles claimed it) + Footer remain — [gallery-orbit.md](gallery-orbit.md)).
>
> **09 Articles rail (`sections/ArticlesSection.tsx` + `articles.tunables.ts` `ARTICLES`)** — the third consumer of the pin + exp-damp-applier engine (Gallery orbit, Skills arrow, now this), and the reference case for **horizontal** travel. Shape: `ScrollTrigger.create({ start: "top top", end: () => "+=" + (distance + innerHeight*tailVh), pin: true, invalidateOnRefresh: true, onUpdate → state.target, onRefresh → measure + converged=false, onToggle → will-change })`, damp tick on the shared `gsap.ticker` applying `quickSetter(track,"x","px")` at `-distance*rendered`. **`distance` is MEASURED** (`track.scrollWidth − innerWidth`), never a tunable — content length sets the pin length. Same tick also drives the ember progress meter (`quickSetter` on `scaleX`) and the counter (written via `ref.textContent`, NOT state — per-frame re-renders otherwise), plus a per-card cover counter-translate from cached `offsetLeft/offsetWidth` (transform-independent, so no un-transform before re-measure). Two things with no other precedent in the repo: (1) **`focusin` → scroll-into-view** — a translated track can focus off-screen cards, so map the card back to a page Y and `lenis.scrollTo(y, { immediate: true })` (eased would lag the caret). **Derive that Y from the card's own measured centre — `clamp01((metrics[i].center − innerWidth/2) / distance)` — NOT from `i/(n-1)`** (corrected 2026-08-01): the even index split assumes the last card sits at progress 1, but `tailVh` owns ~8% of the range, so every card lands short and the last lands past itself. `metrics` is already in closure scope and already holds the answer. The bug was invisible at 4 cards and obvious at 12; (2) the **degenerate-rail guard** — a `ResizeObserver` sets `fitsInView` when the track already fits, which routes to the reduced-motion flow layout instead of pinning to move zero pixels. 01 hero ([hero-refine.md](hero-refine.md)): heroName = exactly TWO word spans (`Muh.`/`Sufyan.`, preloader FLIP contract), tunables `hero/manifesto/about/projects.tunables.ts`. 03 refined ([about-refine.md](about-refine.md)); 04 Projects ([projects-index.md](projects-index.md)). Persisting gotchas: clip wrappers zero flex `min-width:auto`; `pb-[0.12em] -mb-[0.12em]` descender headroom; **`Link` hash scroll passes `{ force: true }`**; **every useGSAP with `prefersReducedMotion`-derived deps needs `revertOnUpdate: true`**. AuroraBackground = ogl shader (uTime from gsap.ticker, never its own rAF). Chrome = MenuButton/MenuPopout/SiteMenu (Header/MobileMenu are deleted names). Chapter 00 = name-morph preloader ([preloader-morph.md](preloader-morph.md)); RootLayout `inert={!preloaderDone}`. Fonts since 2026-07-20: Instrument Serif display (400 only) + Switzer body + Inter labels (`font-mono` utility name kept); accent ember.

## Motion architecture (single sources)
- GSAP registered ONLY in `src/lib/gsap.ts` (registers ScrollTrigger, defaults `ease: "power4.out", duration: 0.8`; exports `gsap`, `ScrollTrigger`). No other file imports `gsap` or `gsap/ScrollTrigger`.
- **Refresh-at-top contract (2026-07-19):** `lib/gsap.ts` sets `history.scrollRestoration = "manual"` **before** `registerPlugin(ScrollTrigger)`, and `main.tsx` calls `window.scrollTo(0, 0)` at module scope (router `scrollRestoration` option removed — it had no other job). Gotcha: ScrollTrigger snapshots `history.scrollRestoration` at init and re-writes the snapshot on EVERY `refresh()` (`_clearScrollMemory`), and import hoisting runs `lib/gsap` before `main.tsx`'s body — so setting "manual" anywhere after registration silently reverts to "auto" at the first refresh (verified live).
- Lenis instantiated ONLY in `src/providers/SmoothScrollProvider.tsx` (lerp 0.09, wheelMultiplier 1, smoothWheel, syncTouch false): `instance.on("scroll", () => ScrollTrigger.update())` (arrow wrapper — direct handoff fails strict TS), `gsap.ticker.add(t => lenis.raf(t*1000))`, `lagSmoothing(0)`; exports `LenisContext`; destroys + removes ticker on cleanup. Under reduced motion Lenis is NEVER created → `useLenis()` (in `src/hooks/useLenis.ts`) returns null.
- No manual resize-refresh listener — ScrollTrigger auto-refreshes on resize. Route-change refresh not wired (single-page site); add only if multi-page ships.
- Provider composition: `src/providers/AppProviders.tsx` = ThemeProvider(defaultTheme="dark") → TooltipProvider → SmoothScrollProvider. `main.tsx` = StrictMode → AppProviders → RouterProvider.
- Every animation in `useGSAP(() => {…}, { scope: ref, dependencies: [...] })`. Returning a function from the useGSAP callback registers a context cleanup (used for `split.revert()`). `invalidateOnRefresh: true` on scrubs.

## Reduced motion (mandatory pattern)
- `src/hooks/usePrefersReducedMotion.ts` — `useSyncExternalStore` on `(prefers-reduced-motion: reduce)`, live-updates on change.
- Pattern: early-return inside the useGSAP callback (content renders in final static state) + conditional classes via `cn()` (e.g. ParallaxImage drops `scale-[1.2]` headroom). Cursor/Marquee return alternate JSX (null / single static track).
- Touch detection: `matchMedia("(hover: hover)")` inside useGSAP (MagneticButton) or once via lazy `useState` (Cursor: `(hover: hover) and (pointer: fine)`).
- `src/hooks/useIsomorphicLayoutEffect.ts` exists for effect-time DOM work.

## Primitive APIs (src/components/common/, all exported from barrel index.tsx)
- `RevealText` — `{ children, mode? = "lines", as? = "div", className?, delay?, stagger?, trigger?: RefObject|string, start? = "top 80%", end?, scrub?: boolean|number, blurFrom? = 0, veiledOpacity? = 0 }` (extended 2026-07-18). `scrub` undefined → shipped once-reveal (yPercent clip rise, `once:true`). `scrub` set → scrubbed REVERSIBLE veiled blur→clear variant, re-split + rebuilt on refresh (mechanics: [about-refine.md](about-refine.md)). **GOTCHA (fixed 2026-07-19): after an onRefresh rebuild the scrub tl MUST `tl.progress(self.progress)`** — a refresh past the trigger end leaves forward progress clamped at 1, the playhead never moves, and the fresh splits stay veiled forever (symptom: resize at page bottom re-blurred the About bio permanently). **GOTCHA: a parent's RefObject is still `null` in this child's layout effect** (React attaches refs walking up) — ancestors pass a SELECTOR (`trigger="#about"`). **split-type caveat: nested spans line-group as ONE atomic unit** — plain text only; mixed-face statements use React-owned word spans (StatementWords pattern).
- `ParallaxImage` — `{ src, alt, width?, height?, aspect? (className), parallax?: ParallaxConfig, withScrim?, className? }`. Figure clip `inset(100% 0 0 0)→inset(0)` (1.2s, once) + img `yPercent from→to` scrub. Raw `<img>` with `scale-[1.2]` headroom (common/ exempt; existing `Image` skeleton wrapper fights the clip). Scrim = `bg-linear-to-b from-transparent from-40% to-ink/65`.
- `Marquee` — `{ children, speed? = 30 (s/loop), reverse?, pauseOnHover? = true, className? }`. Two `.marquee-track` flex tracks (2nd `aria-hidden`), `xPercent 0→-100` repeat -1; tween in a ref, paused/played by React pointer handlers.
- `MagneticButton` — `{ children, className?, strength? = 12 }`. `gsap.quickTo` x/y on wrapper; `.magnetic-label` span counter-moves ×-0.35; spring back `elastic.out(1, 0.4)`.
- `PathDraw` — `{ d, trigger?: RefObject<HTMLElement|null> (defaults own wrapper), strokeWidth? = 3.5 (number → SVG attr; STRING → inline CSS, enables "clamp(1.25rem,5vw,6.5rem)" viewport-tracking widths), className?, viewBox? = "0 0 100 100", preserveAspectRatio? = "none", scrub? = true, start? = "top 80%", end? = "bottom 60%" }`. Bold path draw (§7.2): dash math is `pathLength={1000}` normalized (set dasharray=dashoffset=1000 → tween dashoffset→0, ease none, scrubbed; has `revertOnUpdate: true`). **Two hard-won gotchas (2026-07-20 Craft build): (1) non-scaling-stroke renders dashes in SCREEN space — an anisotropically stretched viewBox tiles the dash into disconnected capsule segments, so scrub callers must pass a PIXEL-SPACE `d` + matching 1:1 viewBox (ProjectsSection measures its layer via ResizeObserver and scales normalized coords with `scalePathD`); (2) pathLength must be 1000, not 1 — GSAP's CSS layer rounds stroke-dashoffset to whole px, quantizing a 0–1 range into a binary step at 50% scrub.** `stroke="currentColor"` + wrapper `cn("text-accent", className)`; `strokeLinecap="round"`; svg `aria-hidden focusable="false"`. Reduced motion: early return → renders fully drawn. Wired into 04 Projects (thread + finale); Journey rail next.
- `ChapterEyebrow` — `{ index, label, className? }`, NOT animated. `font-mono text-eyebrow`, `text-accent` index, muted em-dash + uppercase label.
- `Cursor` — no props; dot+ring quickTo follow, `data-cursor` pill mode (labels in use: `View Project`, `See Project`). Null on coarse pointer / RM; mounted in `__root.tsx`. **Full mechanics + the xPercent re-bake gotcha: [cursor.md](cursor.md).**
- `Preloader` — `name` prop REQUIRED (`Profile["heroName"]` from `__root.tsx`); name-as-shared-element morph, every load: char-cascade wordmark + real loading gate → per-word FLIP onto the hero h1 → ember→ink sheet wipes with the ATOMIC cut (`setPreloaderDone`) at ink full-cover. Numbers in `preloader.tunables.ts`. Full mechanics (gate, FLIP measurement, sheet order, overlay lifetime, reduced motion): [preloader-morph.md](preloader-morph.md).

## Chapter choreography patterns (built)
- **Hero entrance gating** (`HeroSection.tsx`, morph handoff — no char split): one useGSAP, deps `[preloaderDone, prefersReducedMotion]`, `revertOnUpdate: true`; pre-flip words `gsap.set(autoAlpha:0)` (MEASURABLE, never CSS-hide — the preloader FLIP lands on their gBCR); on flip chrome groups stagger in and the `.hero-aurora` WRAPPER (never AuroraBackground's own root) ramps in. Full choreography + gotchas: [hero-refine.md](hero-refine.md).
- **Hero aurora** (`AuroraBackground.tsx`, section-local, no props — the old 2D-canvas description is DEAD; ogl since 2026-07-08): full-viewport ogl `Triangle` + inline GLSL; chroma = horizontal `uv.x` ramp `[deep, accent, deep]`; `uv.y` is **bottom-origin** (matters for anything mouse-mapped). Tick early-returns on `document.hidden` / IO `offscreen` / `faded` from the scroll-fade ScrollTrigger (which also freezes the loop at `progress === 1`); ResizeObserver sizes the buffer. Reduced motion / WebGL fail: static token gradient, no canvas, zero JS. 2026-07-16 uniform surface (`uIntensity`/`uMidPoint` ×intensity coupling, `uMouse` damped hotspot + idle drift, dpr≤1.5 cap, chroma tuning telemetry): see [hero-refine.md](hero-refine.md).
- **GOTCHA — `gsap.utils.toArray` is context-scoped inside useGSAP callbacks** (root-caused 2026-07-16): called from a tween callback (`onRepeat` etc.) created inside a scoped `useGSAP`, selector strings resolve against the SCOPE ref, not document — cross-tree queries (preloader→hero) silently return `[]`. Use `document.querySelectorAll` for out-of-scope DOM.
- **Menu chrome** (Header deleted 2026-07-08; `MenuButton`/`MenuPopout` z-[60] + `SiteMenu` z-[80]): popout ScrollTrigger + curtain reveal + the **`end:"max"` isActive-flips-at-progress-1 gotcha** — full mechanics: [menu-chrome.md](menu-chrome.md).
- **Manifesto = the WebGL MacBook scroll-story behind a CENTER-BORN entry** (T1 rewritten 2026-07-17 — `gsap.matchMedia` GONE, one breakpoint-agnostic rig): T1 tweens `entry` channels; damp applier on `gsap.ticker` (never a new RAF); T2 channels master over 520vh (`-mt-[100svh]` bridge). StatementWords: spaces BETWEEN the inline-block spans; roman `font-display-lead`, focal `font-display-tail italic`. **Full rig, tunables + all headline traps (fiber `advance()` seconds, no invalidateOnRefresh on channel tweens, autoAlpha on the GL stage, veil backdrop root): [manifesto-3d.md](manifesto-3d.md).**
- **About refine (2026-07-18) + stats odometer/ember-glass cards (2026-07-19)**: H1 = React-owned `.about-word` spans with runtime line-bucketed blur; bio = extended RevealText on a gated runway; stats finale = ONE scrubbed master with per-card staircase ignition, StatCard markup = SETTLED state so RM is free. Tunables `about.tunables.ts` (`ABOUT_REFINE`). Full mechanics, invariants + the 5 odometer gotchas: [about-refine.md](about-refine.md).
- **Projects scroll-activated index (04, 2026-07-20; renamed from Craft same day)**: focal-band triggers → activeIndex, badge-carrying `grid-rows` expand, no-revert autoAlpha crossfade, quickTo mouse tilt (listeners on the SECTION), PathDraw thread + 90svh finale. Full mechanics + gotchas (incl. react-icons `SiCss` types-vs-runtime trap): [projects-index.md](projects-index.md). **Revision (2026-07-20, owner)**: 12 rows (6 GitHub-sourced entries), tilt retargeted to `.projects-preview-tilt` (wraps the counter+"Preview" meta row AND the frame so the meta tilts too; `overflow-hidden rounded-lg` stays on the inner frame); the sticky wrapper is now `pointer-events-auto` + `data-cursor="See Project"` (outer panel still `pointer-events-none aria-hidden`; cursor pill is feedback only — preview is NOT a link, a focusable link inside aria-hidden would be an a11y bug); titles always `text-paper-bright` (active = scale only); badges = light-glass pills (`border-paper/15 bg-paper/10 backdrop-blur-md rounded-full`); meta left slot renders `project.year ?? pad2(counter)` (year-ready).
- **Journey zigzag conveyor (08, 2026-07-22; overdrive rework — center-spine v1 superseded same day)** (`sections/JourneySection.tsx`, `journey.tunables.ts`): intro = gallery-clone word de-veil (centered word-span h2, words `autoAlpha 0 + blur(6px)` → clear on a scrubbed window `JOURNEY.heading`, visible-by-markup under reduced motion — NOT RevealText) → THICK serpentine PathDraw (04-thread string strokeWidth, normalized d × **`scalePathD` in `features/home/utils/path.ts`**, shared 04+08) → 6 cards scrub-sliding from each sweep's arrival side + 3 award hover-invert pills. **Conveyor pattern: per-row `gsap.from` with NUMERIC `scrub` (1.2) = GSAP-native damped, reversible reveal — no master/beat math needed when rows are independent.** **Gotcha: scrubbed x-nudges (±120px) expand the scrollable overflow → horizontal scroll on mobile; `overflow-x-clip` on the section (clip, NOT hidden — no scroll container) is the fix.** Also: hover padding via `-m-4 p-4` on a positioned li shifts its border box (v1 lesson — keep hover chrome on inner wrappers). **Moving-dot-card frame pass (2026-07-23)**: swapped the bottom bloom/aura/edge-bars for a top-left radial glow + data-driven frame layers + a `.journey-dot` that orbits the frame's 4 anchor points via ONE repeating (`repeat:-1`), unscrubbed `useGSAP` timeline (4 `.to()` legs, `ease:"none"`) — **reusable pattern for any future "patrol the perimeter" loop: anchor the loop's waypoints to the SAME tunable the static decoration uses** (`JOURNEY.frame.inset`), not an independent percentage — a first %-based pass sent the dot's straight edge-legs through the card's own text column. Reduced motion: no timeline at all, dot stays at its JSX default (bottom-center, static). **Full-frame + sloped-entry pass, same day** (`logs/feature-changes/2026-07-23-journey-full-frame-line-entry.md`): the 4 corner L-brackets are RETIRED (`CARD_CORNERS`, `frame.bracket*` are dead names) — the card frame is now `CARD_FRAME_LINES`, 4 FULL-SPAN solid hairlines at `frame.inset` (20px) whose tails the card's `rounded-card overflow-hidden` clips at the rounded edge; the dot needed only the `bracketInset`→`inset` rename and consequently now rides ALONG those lines, which is the payoff of the anchor-to-one-tunable rule above. `buildZigzagPath`'s entry is no longer flat: it starts at the layer's top-left (`M entryX 0`) and slopes into the first tip using the SAME control-point grammar as the exit sweep (each end holds its x at the shared mid-y) — **mirror the existing exit block rather than inventing a second curve idiom**; a new `line.entryRunway` spacer (mirrors `finaleRunway`, sits inside the line layer's positioning parent BEFORE the `<ul>`) gives the slope its descent, and the measured tips shift with it for free. **Gradient-card pass 2026-07-24** (`logs/feature-changes/2026-07-24-journey-gradient-card.md`): the frame + orbiting dot + top-left glow are RETIRED (`CARD_FRAME_LINES`, `JOURNEY.frame`, `JOURNEY.dot`, `.journey-dot` and its `useGSAP` orbit loop are dead names; the perimeter-patrol recipe above stays valid as a pattern but has no live instance) — cards are now static gradient-card dressing (ink bg, ember bottom bloom, paper edge highlights, halo = box-shadow on the clipped card box: shadows escape their own `overflow-hidden` and ride the tilt transform, spread kept inside `px-page-x` so the section's `overflow-x-clip` can't crop it). Journey card motion is now ONLY the per-card tilt + the scrub reveals; details in frontend-engineer's `journey-cards.md`.
- **Skills arrow + all-open accordion (06, 2026-07-21)** (`sections/SkillsSection.tsx`, `skills.tunables.ts`): ember arrow = ONE damped x-scrub (ScrollTrigger `top top → bottom top` → gallery damp applier → `quickSetter x`). **Reusable insight: enter/exit beats that look element-local are often the sticky h-svh column scrolling in/out — layout owns y, engine owns x** (arrow = flow child after the CTA: ≈66svh tall, clamps below CTA at 700px). Accordion toggle = pure CSS (Radix Presence + `--animate-accordion-down/up` on `--radix-accordion-content-height`, globals.css); entry = one-shot group/item stagger `once:true`, then user-interactive only. Full anatomy/decisions: `logs/feature-changes/2026-07-21-skills.md`.
- **Gallery cover orbit (07, 2026-07-20)** (`sections/GallerySection.tsx`, tunables `gallery.tunables.ts`): images-only scrub-bound cover orbit, damp applier on gsap.ticker (zero drift at rest), sin-depth z straddling the heading's static z-20 mid-plane, StatementWords heading de-veiled by a paused tl driven with `.progress()`. Full engine, patterns + traps (deterministic jitter, rem breakpoint reads, RM resize re-place, insertion tolerance): [gallery-orbit.md](gallery-orbit.md).
- **Circle wipe (10 Contact, 2026-08-04)** (`sections/ContactSection.tsx`): viewport-anchored `fixed` disc, scale scrubbed 0→cover on the incoming sheet, hidden at handoff. Supersedes the dome cap. Cover radius MUST be measured off the element (`offsetWidth/2`), never derived from `innerWidth/innerHeight` — CSS `vmax` is layout-viewport, `innerHeight` is visual, and they diverge on mobile. Full recipe, traps + post-wipe beat ordering: [circle-wipe.md](circle-wipe.md).
- **Pin + Lenis**: Lenis drives native window scroll (no transform mode), so ScrollTrigger pins/fixed positioning just work; no `anticipatePin`, no manual resize listener needed.
- **ASCII hands + Footer entrance (2026-08-14)** (`features/home/components/AsciiHands.tsx`, `sections/FooterSection.tsx`): three.js → `<pre>` ASCII rasteriser with a `background-clip:text` pointer spotlight, driven by `gsap.ticker` (no private rAF) and gated by IntersectionObserver + `document.hidden` at 15fps. Footer name reveals by two **centre-out `clipPath` wipes** (letters stationary, mask grows), meta lines wipe L→R with `ease:"none"` so they read as typing — a `once:true` timeline, NOT a scrub. Full recipe, the source-port deltas and tune knobs: [ascii-hands.md](ascii-hands.md).
- **ScrollProgressHUD (2026-07-19, reworked 2026-07-20; replaces the scroll-spy TODO)**: `shared/ScrollProgressHUD.tsx` — left "(22)" page-% + right chapter rail, keys off live `navLinks` anchors. CRITICAL: never cache ScrollTrigger positions in chrome that mounts before the sections — read live in `onUpdate`. Details: [scroll-progress-hud.md](scroll-progress-hud.md).

## Motion tokens (design_system §7; live in src/styles/globals.css @theme)
- `--ease-out` ≈ `power4.out` (the gsap default), `--ease-inout cubic-bezier(0.83,0,0.17,1)`; durations 0.4/0.8/1.2s; Lenis lerp 0.09.
- Type-scale utilities carry size+leading+tracking: `text-display/hero/hero-line/chapter/statement/item/body/eyebrow/index/meta` (any NEW `--text-*` token must be registered in `src/lib/utils.ts` twMerge font-size groups — `text-hero-line` was, 2026-07-16). Accent is EMBER `#E8380F`. Display faces: since 2026-07-20 the v2.1 hero pairing is the SITE-WIDE stack — `--font-display`/`--font-display-tail` = Instrument Serif, `--font-sans`/`--font-display-lead` = Switzer (`public/fonts/switzer/Switzer-Variable.woff2`, replaced the Medium static), `--font-mono` = Inter; Switzer variable + Instrument Serif italic preloaded in index.html. Chrome tokens `--color-paper-bright #F0F0F0` / `--color-line-strong #8A8A8A` / `--color-flash #66EACC` (flash used only by the off-by-default surnameHoverFlash). `--spacing-manifesto-slot` is DEAD (removed 2026-07-13; the 2026-07-16 slot is `.hero-slot` flex-1 + `HERO_REFINE.seam.slotWidthIdle` min-width). `:root --veil-blur` is the manifesto veil's tweened var.
- Avoid the `Text` component for token type styles: its variant baseline (`text-base leading-relaxed text-foreground`) collides with custom `text-*` tokens in twMerge (unknown token classes get misclassified). Use `Box` + explicit classes.

## State & types
- `src/store/useUIStore.ts` — zustand `{ preloaderDone, auroraReady, menuOpen }` + setters. `preloaderDone` = the swap/handoff cue (fires at the preloader's ink-cover REVEAL — RootLayout `inert` releases then; scroll unlocks ~0.27 s later at overlay unmount). `auroraReady` = preloader gate signal, set by AuroraBackground on first rendered frame AND in both no-shader terminal paths (WebGL fail / reduced motion) so the gate can never hang.
- `src/types/motion.ts` — `RevealMode = "lines"|"chars"|"words"`, `ParallaxConfig { from? = -8, to? = 8 }`.
- `src/routes/__root.tsx` — wiring-only `RootComponent` = `<Preloader/> + <Cursor/> + <RootLayout/>`; local unexported component keeps react-refresh lint happy.
- z-scale convention: page content ≤ z-50 < preloader `z-[90]` < cursor `z-[100]` (cursor always highest).
- Still to build: `PageTransition` (only if multi-page ships).

## Tooling (installed 2026-07-06)
- **context7 MCP** (root `.mcp.json`) — current GSAP/ScrollTrigger/Lenis docs; prefer over memory for API details. No GSAP-specific MCP exists (only obscure community ones — skipped).
- **design-taste-frontend** skill (`.claude/skills/design-taste-frontend/`) — MOTION_INTENSITY/VARIANCE/DENSITY dials + GSAP patterns; **impeccable** has `/impeccable animate` for motion passes.

## Decisions log (durable facts only)
- 2026-07-07: theme default flipped `system`→`dark` in AppProviders (dark-first identity; storageKey unchanged `vite-ui-theme`).
- 2026-07-07: `LenisContext` export in SmoothScrollProvider carries an eslint-disable for `react-refresh/only-export-components` (context must live with its owner).
- Known gap (deliberate): native `cursor: pointer` still shows over links while custom cursor is active; add a scoped `* { cursor: none }` in QA if it bothers.
- design_system v2 authoritative — accent ember `#E8380F` live. Aurora SHIPPED, PathDraw BUILT (wires in at 04→05). Footer ornament converge is DEAD — 2026-08-14 the owner's reference/footer-refine.mp4 replaced it with the ASCII hands field. Borrowed components via `.agents/skills/animated-ui-references` — never install framer-motion.

## 09 Achievements row wipe (2026-08-25) — `sections/AchievementsSection.tsx` + `ACHIEVEMENTS` tunables

The repo's first **per-row-trigger** chapter, and the counter-example to the pin + exp-damp engine: no pin, no scrub, no ticker. Each row owns `ScrollTrigger.create({ trigger: row, start: "top 88%", end: "top 22%", animation: tl, toggleActions: "play reverse play reverse" })` over a paused 2-tween timeline (`.ach-fill` `scaleX 0→1` origin-left + `.ach-text` opacity `0.6→1` at `"<"`).

- **The stagger is emergent, not authored.** Rows are ~91px apart, so each crosses its own trigger line in sequence — that IS the reference's top-down wave. Don't add `stagger` on top of it.
- **`"play reverse play reverse"` is the whole reverse behaviour** the reference shows (row empties once its top passes `end`, refills on scroll-back). Owner picked it over fill-once, 2026-08-25.
- **Settled state is the FILLED row**, so markup renders filled and the motion branch sets the idle state (`gsap.set` scaleX 0 / opacity .6). Same rule as Articles' words: never hide in CSS. Reduced motion therefore falls through to a readable static light table with zero extra branches.
- Returns `() => triggers.forEach(t => t.kill())` from the useGSAP callback; `revertOnUpdate: true` as always.

**GOTCHA — `mix-blend-difference` inversion:** the fill is one blend layer ABOVE the text (difference blends against its *backdrop*, so the text must be underneath), and the section root carries `isolate` so it resolves against that section's `bg-ink` rather than the page. Consequence: **only neutral text can sit under the bar** — `accent-deep` comes back cyan. Anything coloured (the ember hover tick) renders as a sibling AFTER the bar. Documented fallback if compositing ever misbehaves: `clip-path: inset(0 100% 0 0)` on an `aria-hidden` ink copy of the row.

Journey (08) lost its award pills in the same change — every `.journey-row` now carries `data-side`, so the reveal tween's `side ? … : …` guards are gone and `JOURNEY.reveal.awardY` is deleted.

## 10 Workflow rail (2026-08-26) — `sections/WorkflowSection.tsx` + `WORKFLOW` tunables

The pin + exp-damp engine again (Gallery → Articles → here), but it's the variant worth copying next time a rail carries **large text per item**.

**THE INVERSION — read this before "fixing" the markup.** The reference reads as "a track sliding left", and building it literally is the trap: five `--text-chapter` titles inside one transformed track at ~190px spacing overlap each other, and each then needs a counter-translation write to stay legible. So **nothing translates as a group**. Every step is an `absolute inset-0` overlay of the whole stage; only its `.wf-travel` cluster (disc + bubble) takes the per-frame `x`, and `.wf-copy` sits at a fixed centred position and merely crossfades. Same picture, one list, no counter-translation, and no measurement at all.

**Articles' measurement step is deleted, deliberately.** Articles measures `track.scrollWidth − innerWidth` because its travel depends on how many cards exist. Here travel is *choreography* — positions are `(i − playhead) × gap`, pure arithmetic. No track element, no `offsetLeft` metrics array, no `ResizeObserver`. The only per-refresh read is `innerWidth`.

**The playhead map, and why the tail is a real hold.** `TOTAL_VH = headVh + perStepVh×(n−1) + tailVh`; `playhead = clamp01((p − HEAD_FRAC) / TRAVEL_FRAC) × lastIndex` where `TRAVEL_FRAC` **excludes** `tailVh`. That is what makes the tail an actual rest on the last step, rather than Articles' approximation where the tail just slows the final item down. Verified: at progress 1.0 step 5 is still centred and fully lit.

**Two bands, not one — the miss the first build shipped.** Node activeness is `e = smoothstep(1 − min(1, d))` over a 1.0-step band. The COPY runs on its own narrower `copyBand: 0.55`. Sharing the node's band puts two half-lit titles on top of each other mid-transit and renders as one illegible smear; the reference goes fully **dark** between titles for ~2 frames and then brings the new one in. Measured before/after at progress 0.247: nodes 0.51/0.49 (crossfading) while copies sit 0.03/0.02 (dark). If a future rail crossfades big text, give the text its own narrower band.

**Never `autoAlpha` in this chapter — plain `opacity` only.** `autoAlpha` resolves to `visibility: hidden`, which deletes the node from the accessibility tree, and four of five process steps plus the chapter's own `h2` spend most of the pin at opacity 0. Nothing here is interactive so opacity alone costs nothing. Same reason the blur guard writes `filter: "none"` rather than hiding: the a11y requirement and the paint-cost mitigation happen to want the same thing. **This diverges from Articles/Gallery on purpose.**

**Blur budget.** Only steps inside `copyBand` ever carry a `filter`; the string is rewritten only when the rounded px value changes (`lastBlur` guard). `will-change` is `transform` on the travel wrappers + `opacity` on the copies, set only while `isActive` — `filter` is deliberately excluded, since a permanently promoted chapter-scale blurred text layer is exactly the cost the guard exists to avoid.

**`clearProps` in cleanup is mandatory here, and the same hole exists elsewhere.** `quickSetter` writes bypass the GSAP context, so `revertOnUpdate: true` does NOT clear them. Without an explicit `gsap.set([...selectors], { clearProps: "all" })` in the teardown, toggling OS reduced-motion mid-session strands the static list wearing translated/faded/blurred inline styles. Verified both ways (dirty `blur(2px)` + `opacity: 0.711` before the flip → every inline style empty after). **`ArticlesSection` and `GallerySection` have this same latent hole and do not have the fix** — worth one small commit across all three.

**The progress track is static, by construction.** The active node is always at horizontal centre, so the traversed/ahead boundary never moves off 50% — two half-width elements with different `repeating-linear-gradient` cycles, zero per-frame writes. Rejected `scaleX` (stretches the dash *pattern*, not the line) and `clip-path` (a per-frame string for a boundary that is fixed). The gradient phase discontinuity at the seam hides under the disc parked exactly there. Arbitrary values need underscores (`to_right`, `0_10px`) — verify the class survives into the built CSS, a literal space drops it silently.

**Adjacent pinned sections are fine.** Workflow (3.95vh) pins immediately above Articles' pin; measured gap between `workflow.end` and `articles.start` is exactly 1.00 viewport, no dead zone, and never two `position: fixed` sections at once. Standard config: both `start: "top top"` + function `end` + `invalidateOnRefresh`. `onRefresh` MUST reset `converged = false` or the tick's early-return strands old-viewport `gap` px (SkillsSection documented this first; re-verified here with a mid-pin `ScrollTrigger.refresh()`).

### GSAP trap: per-word `stagger` silently resizes a positioned timeline (2026-08-26, ch.10)

Shipped as a real bug and worth generalising. A timeline that mixes **a staggered tween** with
**tweens at absolute positions** is only correct if you know the stagger's total length:

```js
// WRONG — cascade is 0.5 + 0.09*(n-1); at n=19 that is 2.12s, so GSAP sizes the
// timeline to 2.12 and the "0.55" below actually lands at 26% of it.
tl.to(words, { duration: 0.5, stagger: 0.09, ... }, 0);
tl.to(intro, { ..., duration: 0.45 }, 0.55);

// RIGHT — `amount` spreads the STARTS across a fixed window, so cascade length
// is independent of word count and the absolute positions keep meaning.
tl.to(words, { duration: 0.22, stagger: { amount: 0.38 }, ... }, 0);  // ends 0.60
tl.to(intro, { ..., duration: 0.32 }, 0.68);                          // ends 1.00
```

Symptom: the *next* beat starts on top of a half-finished reveal, with no error and no console
noise — the numbers simply mean a different thing than they read. Use `stagger: { amount }` in ANY
timeline whose other children are positioned absolutely, and when the mapping assumes a unit-length
timeline, assert it (`if (import.meta.env.DEV && Math.abs(tl.duration() - 1) > 1e-6) console.warn(...)`).

**`ArticlesSection` uses the per-word form today.** It is safe only because its `headingTl` has a
single child, so normalising over the longer duration is harmless — it acquires this exact bug the
moment a second positioned tween is added. Fix it there in the same commit if you ever do.

Related sizing rule from the same pass: `pin.headVh` must cover the cascade **plus** a read beat.
Workflow's cascade ends at 60% of the head beat and the remaining 40% is where the statement sits
legible; at the original 0.6vh that read beat was under a quarter-viewport of scroll (now 0.8vh).

### "Add an N-second delay" inside a SCRUBBED chapter (2026-08-26, ch.10)

Owner asks of this shape arrive regularly and the literal reading is always wrong here. A scrubbed
timeline has no wall clock — `progress()` is a pure function of scroll position. A real timed hold
can only be built by freezing the page (scrolling visibly does nothing → reads as broken) or by
letting the pin advance while the content stays hidden (the content then surfaces already
mid-travel). **The correct translation is a hold measured in scroll distance**, i.e. an empty
stretch of timeline the visitor crosses in about that long at a normal scroll rate.

Working conversion, used for ch.10's read beat: **~600 px/s** is a fair normal-reading scroll rate
for a wheel or trackpad, so `0.7vh` ≈ 630px @900 ≈ **1.05s**. State the rate assumption when you
quote a duration — it is the soft part, not the implementation.

**Structural lesson, more important than the number.** Don't hand-write timeline positions as
literals when they encode phases. Split the beat into named vh tunables and DERIVE every position
as a share of their sum:

```ts
const HEAD_VH = P.cascadeVh + P.readVh + P.handoffVh;
const CASCADE_END = P.cascadeVh / HEAD_VH;
const HANDOFF_AT  = (P.cascadeVh + P.readVh) / HEAD_VH;
const HANDOFF_DUR = P.handoffVh / HEAD_VH;   // the three sum to 1 BY CONSTRUCTION
```

The hold is then literally **empty timeline** between `CASCADE_END` and `HANDOFF_AT` — nothing
scheduled across it, which is what makes it a hold and not a slow tween. This is also the permanent
fix for the desync class documented above: with derived positions, retuning one phase cannot
silently move another. ch.10 shipped that bug once with hand-written `0.55`/`0.68` literals.
