# About refine choreography (2026-07-19, v4) — tall sticky-portrait section, word-blur reveal, beat triggers, ember glow

Tunables: `src/features/home/utils/about.tunables.ts` (`ABOUT_REFINE` — headline/description/finale/portrait, `damp`, `veiledOpacity`, `blurEnabled`). Headline spans are fractions of About's entry progress (`top bottom` → `top ${overlap*100}%`; `overlap` stays in `MANIFESTO_ENTRY.veil.aboutResolve` — the G seam authority; its `blurFromPx` was deleted).

## v5 structure — reference-height section (~1.53×vh), beats in flow, sticky portrait

- Section ≈ 1.53 viewports (reference traversal target 1.5–1.6): ONE shared beat gap `mt-[12svh] lg:mt-[14svh]` on BOTH the bio and `.about-final` (owner rule: heading→description gap ≡ description→stats gap), finale `lg:mb-[24svh]`. Statement + bio share a view (reference Image #11). Beats in document order: word-blur headline → description (+CV) → `.about-final` (stats as GLASS-WITH-COLOR cards since 2026-07-19 — ember plate rotated behind a smoked `bg-ink/40` glass front, Instrument Serif italic digits; full structure + gotchas in the odometer bullet of [MEMORY.md](MEMORY.md) — ABOVE `→ V3.0` from `siteConfig.version`).
- **Sticky portrait**: rail `absolute inset-y-0 right-0 w-[40vw]` + inner `sticky top-[12svh] h-[88svh]`. **GOTCHA (load-bearing): `overflow-hidden` on the section silently kills `position:sticky`** (the ancestor becomes the containment context and never scrolls) — the section uses **`overflow-clip`**, which crops the halo bleed identically but creates no scroll container, so sticky works. Ancestor transforms (the `.about-item` entry `y:24`) do NOT break sticky.
- Beat triggers: bio RevealText on its OWN root (`description.reveal { start, spanVh }` → end `+=spanVh·vh`); finale reveal = autoAlpha/y/blur fromTo + in-timeline `filter:"none"` set over `finale { start: "top 90%", end: "top 70%" }` (trigger `.about-final`, `scrub: 3/damp`). **Portrait blur/vignette resolve is SEPARATE (v5)**: `trigger: section, "top bottom" → += veil.resolveSpan × (sectionH + vh)` — veiled only during entry, clean orange box from ~20% traversal onward. The last-section constraint bites the finale: its end anchor must resolve ABOVE max scroll or the hygiene `.set` at progress 1 never fires (tuned 45→62→70% across height changes; re-tune when chapter 04 lands after About and the constraint disappears).
- **Hero overflow bug FIXED (2026-07-19, re-applied with the portrait task)**: `overflow-x-clip` on the hero section root — the manifesto zoom-exit h1 no longer widens the document. Verified twice: FLIP morph + zoom beat visually identical (clip edge = viewport edge); phones no longer zoom out, so the page tail (mobile portrait reveal + finale tail) is reachable. Emulation gotcha: under DevTools MOBILE emulation `window.innerHeight` is the (zoom-dependent) visual viewport, not the layout viewport — compute scroll math from `documentElement.clientHeight`, or scripted scrolls under-shoot and once-triggers look "dead".

## H1 — statement word-blur (word spans, no split-type, v2 word-sequential)

- Statement = React-owned `.about-word` inline-block spans from `toWords()` (char-range intersection against `aboutStatementEmphasis` — word-membership would mis-flag the roman "and"). Focal words `font-display-tail italic`; roman inherits `font-display-lead` from the h2. Literal `" "` text nodes BETWEEN spans (StatementWords space rule). h2 `aria-label` + inner `aria-hidden` = single AT exposure. `lg:max-w-[72%]` deliberately overflows the z-10 text column onto the portrait panel (reference).
- **v2 timing (supersedes v1 line bucketing — `groupLines()`/`onRefresh` rebuild DELETED)**: words stagger INDIVIDUALLY in DOM order across `headline.span` — `step = spanLen/(n−1+trail)`, `dur = trail·step` (trail = words concurrently mid-resolve; last word settles exactly at `span[1]`). The sharp/blur boundary travels MID-LINE like the reference, and the timeline is layout-independent → build once, no refresh rebuild, no offsetTop measurement. Span sits at [0.3, 0.75] so the resolve is VISIBLE mid-viewport (v1's [0.05,0.45] completed below the fold — the owner caught it).
- Per word: fromTo veiled→clear + in-timeline `.set(filter:"none")` at its settle (finite blur, symmetric on reverse). ~14 glyph-sized filter surfaces, all removed at rest.
- **Invariant: every timeline child is fromTo/set with both ends explicit.** Entry trigger stays `scrub: true` (numeric scrub would lag About behind the veil it must stay in lockstep with); wrapper condense kept but has NO filter tween (word blurs are the blur story).

## H2 — description reveal (v4: own-position trigger; the v2 runway math is DELETED)

- Bio = extended `RevealText` (`scrub = 3/ABOUT_REFINE.damp` ≈ 0.6 s), `blurFrom`, `veiledOpacity`, triggering on its OWN root: `start = description.reveal.start`, `end = "+=" + spanVh·innerHeight`. The headline→description gate is physical layout distance now (the bio sits ~1 viewport below the statement).
- Two-level reveal: the `p` itself is still an `.about-item` (alpha in with the entry stagger); its LINES sit veiled (opacity `veiledOpacity` + blur) until its own scrub resolves them. Different elements → no property conflict.
- CV action (2026-07-19): same veil grammar inline on the `.about-cv` wrapper (NOT an `.about-item` — the veil is its pre-state; double-tweening opacity would conflict). Unsplittable target (nested Link + arrow) → single fromTo + hygiene set, `y: SCRUB_Y` (now exported from RevealText), own trigger at `D.reveal.start`, span `spanVh/2`·vh ≈ one bio line's resolve.

## D4 — portrait (`EmberPortrait` local component, both viewport variants)

- **v11 = photo under the ember DUOTONE filter, LIVE (2026-07-19; v10 natural → owner asked the orange filter back same day — this owner does revert/re-request round-trips, cf. v7/v8)**: real asset `public/assets/images/profile/about-profile.png` (824×1280 PNG 1.6MB — WebP conversion open). Layer stack inside the rounded `.about-clip`: img → `.about-tint` (`mix-blend-color` over `TINT_GRADIENT` from `portrait.tint.stops ["accent-deep","accent"]` — luminance preserved, hue fully ember, photo READABLE) → `.about-glare-in` (GSAP entrance band) → `.about-glare` (CSS hover band). The v8-era opaque normal-blend WASH stays deleted — re-adding it hides the photo entirely. The clip wrapper OWNS the corner radius + the thin static ember glow `shadow-[0_0_40px_-8px_var(--color-ember-glow-deep)]` and carries `group`. Wrapper rides the `.about-item` entry stagger; sticky wrapper `sticky top-[6svh] h-[88svh]`.
- GLARE (React Bits GlareHover, adapted per animated-ui-references; example-tuned −30° / paper/30 / 300%, duration 2s per owner 2026-07-19): ONE shared class recipe `GLARE_BAND` in AboutSection. HOVER = pure CSS on `.about-glare` (`bg-position-[-100%_-100%]` → `group-hover:bg-position-[100%_100%]`, `transition-[background-position] duration-2000`). ENTRANCE = `.about-glare-in`, a SEPARATE band (a GSAP write on the hover band would fight its CSS transition): per-clip `fromTo` of `backgroundPosition` "-100% -100%"→"100% 100%", `once: true`, `start: portrait.glareIn.start` (= `"clamp(top 65%)"` — last-section protection), time-based `dur` = `portrait.glareIn.dur` (2 — keep in sync with the hover `duration-2000`). Both bands `motion-reduce:hidden` + guarded by the section's reduced-motion early return.
- **Load-bearing pointer-events fix**: the full-width z-10 `.about-inner` column swallowed ALL hover over the portrait rail (glare hover was dead on desktop through v9). `.about-inner` is `pointer-events-none` + `[&>*]:pointer-events-auto` — children interactive, empty column space falls through to the rail. Don't remove either half.
- (History: v3 streaks → v5/v6 focus-blur/vignette/halo → v8/v9 opaque orange placeholder box — all deleted; the built-then-reverted round-trip is `logs/feature-changes/2026-07-19-portrait-photo-glare{,-reapply}.md`. If a portrait veil ever returns: blur the CLIP WRAPPER, scrub over section-traversal fractions, hygiene `.set(filter:"none")` at span end.)
- Stats staircase cap: the `.about-final` stats column uses `lg:max-w-[calc(60vw_-_2*var(--spacing-page-x))]` — ends exactly one page-x short of the 40vw portrait rail, so `self-end` cards never cross into the panel.
- `blurEnabled: false` (single low-perf switch): H1/H2/finale drop the filter channel (opacity+y reveals, same timings).

## Verified invariants (MCP, 2026-07-19 v4)

- Reverse pass reproduces down-pass states exactly (scrub-reversible end to end — word boundary, bio lines, finale, portrait blur/vignette all symmetric).
- At max scroll: **ZERO active filters** anywhere in #about (the v3 distortion whitelist is gone) and zero `will-change` — every hygiene `.set(filter:"none")` fires before scroll exhausts.
- Sticky proof: portrait rect identical across mid-section positions (top = 12svh), flush bottom at the section end; survives reduced motion (pure CSS).
- Seam unchanged: veil 1.00 / tint 0.00 past T2, About resolving inside the tail; entry trigger geometry untouched through all four revisions.
- Markup defaults ARE the settled state → the single reduced-motion early return renders complete (bio unsplit, finale visible, halo idle, vignette 0, portrait sharp) with no per-branch styling.

## Stats odometer + ember-glass cards (2026-07-19, moved from MEMORY.md index)

- **Odometer (/impeccable overdrive):** finale = ONE scrubbed master timeline (`ABOUT_REFINE.stats` replaced the `finale` key) — per-card staircase ignition (shell blur-resolve → SVG rect `pathLength=100` dashoffset border-draw → digit-strip roll → glare xPercent sweep), version line (`.about-version`) on the tail; the tail's `filter:"none"` set is what the last-section anchor guards (end `top 20%`, verified ≤ maxScroll both breakpoints). StatCard markup = SETTLED state (inline `translateY(−100·(len−1)/len%)` strips, clean rect, glare `opacity-0`) so reduced motion is free.
- GOTCHAS: (1) **a settled percent transform parses into GSAP's `y` channel as PIXELS — strip fromTo must zero BOTH `{ y: 0, yPercent: 0 }`** or strips start offset; (2) dash channels live ONLY on the motion path + in-timeline `strokeDasharray:"none"` set at draw-end (kills DPR dash seam, re-applies on reverse); (3) glare parks OUTSIDE the card's overflow-clip at opacity 1 → sweep is pure transform; (4) deliberately NOT PathDraw (own trigger would desync from the master beats); (5) trigger window must account for the block's own height — `top 40%` end settled card 2 below the fold, `top 20%` + cardStagger 0.85 puts the "10" crescendo on-screen at 900px.
- **Glass-with-color restyle (React Bits GlassIcons via animated-ui-references):** `.stat-card` is now the WRAPPER (shell beat ignites both children) of an ember plate (`rotate-[6deg]` origin-bottom-right, `from-accent to-accent-deep`, ember-glow shadow) + a smoked-glass front (`bg-ink/40 backdrop-blur-md` — light `paper/*` glass FAILS AA for in-card text over the plate; owner-approved). Digits `font-display-tail italic` (Instrument Serif — `italic` is load-bearing, the only hosted face is italic); slots `overflow-x-visible overflow-y-clip` (serif overhang; visible+clip is the valid single-axis combo). Hover = pure CSS `motion-safe:group-hover` with native rotate/translate/scale utilities ONLY (**Tailwind v4 individual transform props COMPOUND with arbitrary `[transform:…]` on the same element**); the wrapper, `.stat-strip` and `.stat-glare` must stay transition-free (GSAP writes them every scrub frame). Plate is 6°/10°, not the source's 15°/25° — tip lift scales with card width and would cross the previous staircase card. Stat labels honor `\n` (`whitespace-pre-line`) — line-break card labels in `profile.data.ts`, not in markup.
