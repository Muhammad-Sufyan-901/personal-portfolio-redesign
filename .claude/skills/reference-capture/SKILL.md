---
name: reference-capture
description: Capture lukebaffait.fr (or any scroll-animated reference site) as still images Claude's vision can read — video and animated GIFs don't work. Use before any visual-reference analysis or when a new reference recording arrives.
---

# reference-capture (stub — canonical in `.agents/skills/reference-capture/`)

KNOWLEDGE skill: read the canonical `.agents/skills/reference-capture/SKILL.md`.
The real executables live with the reference data (not duplicated here):
`reference/scripts/extract-frames.mjs` (CONFIG-driven video→still sets, `--probe` mode),
`capture-lukebaffait.mjs` (live crawl, `npm run capture:ref`), `histogram-phases.py`
(palette evidence). Set inventory: `reference/REFERENCE-NOTES.md`.
