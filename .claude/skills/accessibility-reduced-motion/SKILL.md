---
name: accessibility-reduced-motion
description: Accessibility + prefers-reduced-motion handling. Use for every animated component and before any QA sign-off.
---

# accessibility-reduced-motion (stub — canonical in `.agents/skills/accessibility-reduced-motion/`)

KNOWLEDGE skill: read the canonical `.agents/skills/accessibility-reduced-motion/SKILL.md`.
Claude-Code-side enforcement: `.claude/rules/accessibility.md` (path-scoped — the
mandatory reduced-motion pattern, overlay `inert` containment, focus-ring rules).
