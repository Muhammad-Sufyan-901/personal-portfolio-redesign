---
name: vite-setup
description: Vite config for this React-TS portfolio — alias, Tailwind v4, TanStack Router plugin, fonts. Use during scaffolding or build-config changes.
---

# vite-setup (stub — canonical in `.agents/skills/vite-setup/`)

KNOWLEDGE skill: read the canonical `.agents/skills/vite-setup/SKILL.md`.
Claude-Code-side enforcement: the build-config section of `.claude/rules/project.md`
(plugin order: tanstackRouter → react → tailwindcss).
