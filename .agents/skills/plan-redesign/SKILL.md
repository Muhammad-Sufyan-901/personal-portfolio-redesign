---
name: plan-redesign
description: Stage 1 whole-site planning — read the context docs + as-built state, write/revise PLAN.md, surface open decisions, then STOP for explicit approval. Use at redesign start or when re-planning the remaining scope; never for implementation code.
---

# plan-redesign (stub — canonical in `.claude/skills/plan-redesign/`)

PROCESS skill: the canonical, invokable implementation is `/plan-redesign`
(`.claude/skills/plan-redesign/SKILL.md`) — non-Claude agents read that file directly.
Hard rules preserved there: planning is whole-site, execution is per-section
(`workflows/planning.md`), and it always **STOPs for approval** before any code.
