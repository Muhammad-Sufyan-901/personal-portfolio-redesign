---
name: discover-tooling
description: Search the web for Claude Code skills and MCP servers relevant to this project's stack, evaluate them, and propose the best fits. Use when hunting for new tooling; proposes only — never auto-installs.
---

# discover-tooling (stub — canonical in `.claude/skills/discover-tooling/`)

PROCESS skill: the canonical, invokable implementation is `/discover-tooling [focus]`
(`.claude/skills/discover-tooling/SKILL.md`) — non-Claude agents read that file directly.
Already adopted (don't re-propose): MCP context7 / chrome-devtools / shadcn; vendored
skills impeccable, design-taste-frontend, cinematic-web.
